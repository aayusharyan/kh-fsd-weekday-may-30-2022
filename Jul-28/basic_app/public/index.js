const app = document.getElementById('app'); //Get the DOM reference
const root = ReactDOM.createRoot(app); //Create an React root reference.
root.render(<App />) //Inside root, render the App Component.